# Configuration for GraphSAGE model
defaults:
  - default

model:
  name: "GraphSAGE"
  hidden_dim: 96
  num_layers: 3
  dropout: 0.4
  aggregator: "mean"

dataset:
  n_nodes: 2000
  network_type: "watts_strogatz"

training:
  learning_rate: 0.008
  epochs: 120
